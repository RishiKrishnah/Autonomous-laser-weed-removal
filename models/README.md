# Models

Trained weights are intentionally not committed.

After training, the expected file is:

```text
runs/weed_detector/weights/best.pt
```

For GitHub releases, use Git LFS or a model-hosting service if the trained weights are large. Record the exact training command and dataset version in `docs/EXPERIMENTS.md`.
